name: flutter_patcher
description: Open-source, self-hosted code push (OTA updates) for Flutter Android. Patches Dart AOT libapp.so and assets on cold start with integrity checks and crash rollback.
version: 0.1.4
homepage: https://github.com/xuelinger2333/flutter_patcher
repository: https://github.com/xuelinger2333/flutter_patcher
issue_tracker: https://github.com/xuelinger2333/flutter_patcher/issues
documentation: https://github.com/xuelinger2333/flutter_patcher#readme

topics:
  - flutter
  - android
  - code-push
  - hot-update
  - ota-update

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_patcher_core:
    path: packages/core
  flutter_patcher_client:
    path: packages/client
  plugin_platform_interface: '>=2.0.2 <3.0.0'
  # `dart run flutter_patcher:pack` CLI 依赖 —— 运行时 app 里都会被 tree-shake 掉
  archive: '>=3.4.10 <5.0.0'
  args: '>=2.4.0 <3.0.0'
  crypto: '>=3.0.3 <4.0.0'
  # `bin/pack.dart` executable depends on the CLI library (kept in
  # `dependencies`, not `dev_dependencies`, so the lint
  # `depend_on_referenced_packages` stays quiet for the bin/ entrypoint).
  flutter_patcher_cli:
    path: packages/cli-tools

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.flutter_patcher.flutter_patcher
        pluginClass: FlutterPatcherPlugin
