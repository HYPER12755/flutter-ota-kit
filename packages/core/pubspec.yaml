name: flutter_patcher_core
description: Core data model, rollout cohorts, and shared utilities for flutter_patcher — Dart translation of hot-updater's packages/core.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
dev_dependencies:
  test: ^1.31.2
